# Full Presidio architecture — matches presidio-analyzer/pyproject.toml
# (minus `click`, which is only used by Presidio's CLI).
spacy>=3.4.4,!=3.7.0,!=3.8.14,<4.0.0
numpy>=1.19.0,<2.6.0
regex>=2023.0.0
tldextract>=5.0.0
pyyaml>=6.0.3,<7.0.0
phonenumbers>=8.13.0
pydantic>=2.12.5,<3.0.0

# presidio-anonymizer: required by the encrypt/decrypt (AES) operators.
cryptography>=3.4.7

# A spaCy model is also required — not installable via requirements.txt:
#   python -m spacy download en_core_web_sm    (13 MB, what we use)
#   python -m spacy download en_core_web_lg    (560 MB, Presidio's default)
